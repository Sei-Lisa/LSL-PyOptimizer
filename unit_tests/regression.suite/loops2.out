default
{
    timer()
    {
        do
            ;
        while (llGetLinkNumber());
        for (; llGetLinkNumber(); )
            ;
        do
            ;
        while (llGetLinkNumber());
        llOwnerSay("blah");
        do
            ;
        while (1);
    }
}
